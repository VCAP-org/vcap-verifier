{
  "name": "vcap-verifier-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The static verifier page: one HTML file, one bundle, nothing leaves the browser.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "node build.mjs",
    "dev": "node build.mjs --serve",
    "test:e2e": "playwright test",
    "test": "vitest run"
  },
  "dependencies": {
    "vcap-verify-core": "*"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.25.12",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  },
  "license": "MIT"
}
