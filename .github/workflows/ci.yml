name: ci
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      # TODO: npm ci && npm test && run the vcap-spec conformance vectors.
      - run: echo "no core yet"
