# 24-heic-existing-free-box

The media already ends with an unrelated free box. Only the vcap trailer (identified by its footer) is stripped; the other free box is content.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
