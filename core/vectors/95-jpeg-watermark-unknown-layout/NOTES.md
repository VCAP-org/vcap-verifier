# 95-jpeg-watermark-unknown-layout

A core declaring `watermark.layout: photo-bch-v4`, a layout this version does not define. The field is extensible (§9): the proof is schema-valid and the verdict is the ordinary one, with *watermark not evaluated* — the answer §8 gives for a layout a verifier does not implement, never a refusal of the file.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
