# 29-seg-chain-sig-high-s

sig(0) replaced by its malleated twin (r, n − s). It is still a valid signature for message(0), and the chain hashes messages, not signatures (§5), so nothing breaks. Under the earlier draft (prev_link = SHA-256(sig)) this genuine original would have read as tampered.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
