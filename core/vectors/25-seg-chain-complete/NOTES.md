# 25-seg-chain-complete

Three segments, all present, chain unbroken from index 0, count matches. debug.messages_hex gives the 96-byte messages so an implementation can compare byte for byte before touching signatures.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
