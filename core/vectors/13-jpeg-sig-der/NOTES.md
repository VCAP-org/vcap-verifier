# 13-jpeg-sig-der

A DER-encoded ECDSA signature instead of P1363. Not 64 bytes: the signature is malformed, and a present-but-invalid signature is tampered (§8). A verifier that transparently accepts DER fails conformance.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
