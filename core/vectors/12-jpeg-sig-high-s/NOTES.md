# 12-jpeg-sig-high-s

The signature s value is in the high half of the group order. Writers MUST emit low s; verifiers MUST accept both (§4.2). A verifier that rejects this fails conformance.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
