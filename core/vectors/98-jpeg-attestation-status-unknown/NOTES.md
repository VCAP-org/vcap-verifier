# 98-jpeg-attestation-status-unknown

The snapshot says `unknown` for the intermediate. That is the registry not knowing, and a verifier that read it as revoked — the reference verifier did — would turn silence into an accusation. **Amber**, *chain revocation not checked*, the level stands (§6.2).

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
