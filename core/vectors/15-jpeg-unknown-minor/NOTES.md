# 15-jpeg-unknown-minor

v is vcap/1.7 and there is an unknown top-level key. Same major: verify what you know, list the unknown key as not evaluated, never fail (§9). The unknown key is outside the core, so the signature is unaffected.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
