# 31-seg-chain-prev-link-forged

Segment 2 carries a prev_link that is not SHA-256(message(1)) while segment 1 is present. Contiguity is claimed and broken: tampered, even though sig(2) itself might have been valid over the forged message.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
