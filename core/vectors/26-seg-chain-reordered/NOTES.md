# 26-seg-chain-reordered

Segments 1 and 2 swapped (content, prev and sig of one presented under the index of the other). The stored prev_link of the segment now at index 1 is not SHA-256(message(0)): chain broken where contiguity is claimed.

Generated by `tools/src/generate.ts` with the test key in `tools/src/testkey.ts`.
