{
  "name": "vcap-verify-cli",
  "version": "0.1.0",
  "description": "Verify a vcap proof from a shell: a readable verdict, JSON for machines, and an exit code a script can branch on.",
  "type": "module",
  "bin": {
    "vcap-verify": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "vcap-verify-core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "license": "MIT"
}
